Q1. int max(int x[]) ✅
Q2. int min(int x[]) ✅
Q3. int max1(int x[][])✅
Q4. int min1(int x[][]) ✅
Q5. void sort(int x[]) ✅
Q6. void sort1(int x[][]) ✅
Q7. void mergesort(int x[], int y[])✅
Q8. void union(int x[], int y[]) ✅
Q9. void intersection(int x[], int y[]) ✅
Q10. void matrixadd(int x[][], int y[][])✅
Q11. void matrixmult(int x[][], int y[][]) ❌
Q12. void matrixtranspose(int x[][]) ✅
Q13. void matrixsum(int x[][]) ✅
Q14. void triangleAsum(int x[][]) ✅
Q15. void triangleAmax(int x[][])  ✅
Q16. void triangleAmin(int x[][]) ✅
Q17. void triangleBsum(int x[][])  ✅
Q18. void triangleBmax(int x[][])  ✅
Q19. void triangleBmin(int x[][]) ✅
Q20. void shift(int x[]) ✅
Q21. void frequencycount(int x[]) ✅
Q22. void firstnonrepeatingelement(int x[])✅
Q23. int binarytodecimal(int x[]) ✅
Q24. void maxinonerow(int x[][]) ✅
Q25. void numtoword(int x[])  ❌
Q26. void printdiagonalwise(int x[]) 
Q27. void sequencediagonalwise(int x[][]) 
Q28. void printtrianglewise(int x[]) 
Q29. void LCM(int x[]) 
Q30. void HCF(int x[])
Q31. int find3rdLargestValueInArray(int z[]) (without sorting)
Q32. float findMedian(int z[])
Q33. float findMean(int z[])
Q34. float findMode(int z[])
Q35. void makeAllElementZero(int x[5][5])


Q36. Find Kth smallest element WAP to find Kth smallest element in unsorted Array.

Input: arr[] = {7, 10, 4, 3, 20, 15}, K = 3

Output: 7

Input: arr[] = {7, 10, 4, 3, 20, 15}, K = 4

Output: 10

(Q37 is missing from the images)

Q38. Majority Element Write a java program to get the majority element from an given array of integers containing duplicates.

Definition: A majority element is an element that appears more than n/2 times where n is the size of the array.

Input: arr[] = {1, 1, 2, 3, 1, 5, 3, 1, 1, 1}

Output: 1

Q39. Longest Consecutive Sequence Write a java program to find the length of the longest consecutive elements sequence from a given unsorted array of integers.

Input: arr[] = {49, 1, 3, 200, 2, 4, 70, 5}

Output: The longest consecutive elements sequence is [1, 2, 3, 4, 5], therefore program will return its length.

Q40. Max Product of Three Numbers An unsorted array of integers is given, you need to find the max product formed by multiplying three numbers. (You can not sort the array, watch out when there are negative numbers).

Input: array[] = {2, 5, -2, 6, -3, 8, 0, -7, -9, 4}

Q41. Non-repeated Numbers Find the numbers which is not repeated in array of integers, others are present for two times.

Input: 23, 34, 56, 21, 21, 56, 78, 23, 34

Q42. Sort Array based on another Array Given two arrays, sort the values of one array using the second array.

Input:

String array1[] = {"a", "b", "c", "d", "e", "f", "g", "h", "i"}

int array2[] = {0, 1, 1, 0, 1, 2, 2, 0, 1}

Output: {"a", "d", "h", "b", "c", "e", "i", "f", "g"}

Input:

array1 = {"g", "e", "e", "k", "s", "f", "o", "r", "g", "e", "e", "k", "s"}

array2 = {0, 1, 1, 0, 1, 2, 2, 0, 1}

Output: {"g", "k", "r", "e", "e", "g", "s", "f", "o"}

Q43. Missing Number How to find the missing number in a given Array from number 1 to 100?

Q44. Jagged Arrays What are jagged arrays in java?

Definition: Arrays containing arrays of different length is known as jagged arrays. Multidimensional arrays are also known as jagged arrays.

Q45. Pairs with Sum How to find all pairs on integer array whose sum is equal to given number?

Q46. Remove Duplicates Write a program to remove duplicates from array in Java?

Q47. Element Repeated Twice There is an array with every element repeated twice except one. Find that element?

Q48. Common Elements in 3 Sorted Arrays How to find common elements in three sorted array? Given three arrays sorted in non-decreasing order, print all common elements in these arrays.

Input 1: {1, 5, 10, 20, 40, 80}

Input 2: {6, 7, 20, 80, 100}

Input 3: {3, 4, 15, 20, 30, 70, 80, 120}

Output: 20, 80

Q49. First Repeating Element How to find the first repeating element in an array of integers? Given an array of integers, find the first repeating element in it. We need to find the element that occurs more than once and whose index of the first occurrence is smallest.

Input: {10, 5, 3, 4, 3, 5, 6}

Output: 5 (5 is the first element that repeats)

Q50. Smallest Non-Representable Sum How to find the smallest positive integer value that cannot be represented as sum of any subset of a given array? You have given a sorted array (sorted in non-decreasing order) of positive numbers, find the smallest positive integer value that cannot be represented as sum of elements of any subset of given set. Expected time complexity of O(n).

Input: {1, 3, 6, 10, 11, 15}

Output: 2


